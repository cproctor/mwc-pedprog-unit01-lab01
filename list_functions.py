
def square_list(numbers):
    """Squares each number in a list.
    Arguments:
        numbers (list of integers or floats)
    Returns:
        a list of integers of floats
    """
    raise NotImplementedError("This function isn't done")

def capitalize_list(strings):
    """Capitalizes each string in a list.
    Arguments:
        strings (list of strings)
    Returns:
        a list of strings
    """
    raise NotImplementedError("This function isn't done")

def pluralize_list(numbers):
    """Pluralizes each string in a list.
    Arguments:
        strings (list of strings)
    Returns:
        a list of strings
    """
    raise NotImplementedError("This function isn't done")

def reverse_list(items):
    """Returns a new list whose items are in reverse order.

    Arguments:
        items (list)
    Returns:
        a list 
    """
    raise NotImplementedError("This function isn't done")
